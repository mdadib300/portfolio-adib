const Button = ({btnText}) => {
    return (
        <div>
            <button className="btn btn-wide py-6 bg-[#293241] text-[#e0fbfc]">{btnText}</button>
        </div>
    );
};

export default Button;